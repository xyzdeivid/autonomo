const itemColor = '#330066'
const outflowColor = '#660000'
const entryColor = '#006600'
const homeColor = '#06687E'

export const colors = {

    items: {
        max: itemColor,
        midMax: `${itemColor}BF`,
        mid: `${itemColor}80`,
        midMin: `${itemColor}40`,
        min: `${itemColor}1A`
    },

    outflows: {
        max: outflowColor,
        midMax: `${outflowColor}BF`,
        mid: `${outflowColor}80`,
        midMin: `${outflowColor}40`,
        min: `${outflowColor}1A`
    },

    entries: {
        max: entryColor,
        midMax: `${entryColor}BF`,
        mid: `${entryColor}80`,
        midMin: `${entryColor}40`,
        min: `${entryColor}1A`
    },

    home: {
        max: homeColor,
        midMax: `${homeColor}BF`,
        mid: `${homeColor}80`,
        midMin: `${homeColor}40`,
        min: `${homeColor}1A`
    }

}