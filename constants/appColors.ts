const itemColor = '#330066'
const outflowColor = '#660000' 
const entryColor = '#006600'
const homeColor = '#08819B'

export const colors = {

    items: {
        max: itemColor,
        mid: `${itemColor}80`,
        min: `${itemColor}1A`
    },

    outflows: {
        max: outflowColor,
        mid: `${outflowColor}80`,
        min: `${outflowColor}1A`
    },

    entries: {
        max: entryColor,
        mid: `${entryColor}80`,
        min: `${entryColor}1A`
    },

    home: {
        max: homeColor,
        mid: `${homeColor}80`,
        min: `${homeColor}1A`
    }

}