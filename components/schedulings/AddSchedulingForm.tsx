import FormBody from "../common/FormBody";
import FormContainer from "../common/FormContainer";
import FormTitle from "../common/FormTitle";

export default function AddSchedulingForm() {

    return (
        <FormContainer>
            <FormBody>
                <FormTitle text='Registrar Agendamento' />
            </FormBody>
        </FormContainer>
    )

}